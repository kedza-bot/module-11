#tuples
my_tuple = (1, 2, 3, 4, 5)

print(my_tuple[2])  # Accessing the first element of the tuple using indexing
print(my_tuple[0])  # Accessing the first element of the tuple using indexing
print(my_tuple[-1])  # Accessing the last element of the tuple using negative i
# Tuples are immutable, so we cannot change their elements,you can't change the value of an element in a tuple

tuple = ("Name: Kedza", "Age: 25", "Country: Zimbabwe", "City: Harare", "Occupation: Software Developer")
tuple2 = ("favourite_fruit :Mango", "favourite_color : Blue", "favorite_animal: Dog")

# Concatenating two tuples
con_tuples = tuple + tuple2

tuple3 = (1, 2, 3)  # A tuple with three elements

mul_tuple = tuple3 * 2  # Multiplying the tuple to repeat its elements
print(mul_tuple)  # Printing the concatenated tuple